# Production is pinned to Python 3.12.x.
# Open3D 0.19.0 does not publish wheels for Python 3.13, so do not run
# the production service from the Conda base Python 3.13 environment.
numpy==2.2.6
PyYAML==6.0.3
fastapi==0.141.1
uvicorn[standard]==0.52.1
zivid==2.18.0
open3d==0.19.0; python_version >= '3.12' and python_version < '3.13'